{
  "cycle": 8,
  "phase": "advance",
  "pending_advance_plan": {
    "title": "지출 분석 시트에 '총 예산 대비 총 지출' 요약 카드 추가",
    "why": "cycle 5의 advance에서 DB.budgets(카테고리별 예산 한도)를 openSpendAnalysis()에 연결했지만, 그 화면은 여전히 카테고리 한 줄씩만 보여준다 — 사용자가 예산을 5개 카테고리에 설정했다면 이번 달 전체적으로 괜찮은지 알려면 5개 진행률 막대를 직접 눈으로 합산해야 한다. 화면 맨 위 '총 지출' 카드(index.html:3524)는 예산 개념 없이 전체 지출 합계만 보여줘서, 방금 만든 예산 기능이 '카테고리별로는 보이는데 이번 달 전체로는 안 보이는' 반쪽짜리 상태다. CSV 내보내기(cycle 7)·순자산 추이(cycle 6)와 같은 패턴 — 이미 있는 데이터/인프라를 새로운 집계 관점으로 한 번 더 연결하는 작업이라 한 사이클 안에 안전하게 끝낼 수 있고, 순수함수로 뽑아 회귀 테스트를 붙이기도 좋다.",
    "approach": "1) 순수함수 totalBudgetSummary(rows, budgets)를 추가한다: rows(spendByCategory 결과)와 budgets(DB.budgets)를 받아 예산이 설정된 카테고리만 골라 budgetedTotal(예산 합계)·spentOnBudgeted(해당 카테고리들의 지출 합계)·overCount(예산 초과 카테고리 수)·unsetCount(예산 미설정 카테고리 수)를 반환한다. 예산이 하나도 없으면 budgetedTotal=0을 반환해 '집계할 게 없음'을 표시한다. 2) openSpendAnalysis()에서 이 함수를 호출해, budgetedTotal>0일 때만 기존 '총 지출' 카드 아래(또는 옆)에 두 번째 요약 카드를 추가한다 — budgetProgress()를 재사용해 진행률/초과 여부를 계산하고 '이번 달 예산 320,000원 중 275,000원 사용 · 86%' 같은 문구와 진행바를 보여주며, unsetCount>0이면 '예산 미설정 카테고리 N개' 같은 작은 안내를 덧붙여 집계 범위를 명확히 한다. budgetedTotal===0(예산을 하나도 안 정한 사용자)이면 지금처럼 카드를 추가하지 않아 기존 화면을 그대로 유지한다. 3) test/run.js에 totalBudgetSummary 회귀 테스트를 추가한다: 예산 미설정 전체(0 반환), 일부만 예산 설정, 예산 총합 초과 케이스. 4) APP_VERSION patch bump + sw.js CACHE 버전 갱신."
  },
  "history": [
    {
      "cycle": 8,
      "phase": "critique",
      "timestamp": "2026-09-10T18:00:00+00:00",
      "summary": "전체 앱을 한 걸음 물러나 봤다. 접근성(aria-label 5개뿐, role/alt/tabindex 0개)과 로컬 인증 해시 우려는 이전 사이클(6,7)에서 이미 runner-up으로 반복 언급됐지만, 실제로 코드를 확인해보니 인증은 djb2 로컬 해시가 아니라 Supabase Auth(SB.auth.signUp/signInWithPassword)로 완전히 대체돼 있었다 — 그 runner-up은 더 이상 유효하지 않아 폐기. 대신 cycle 5에서 만든 예산 기능(DB.budgets, openSpendAnalysis 카테고리별 진행률)이 카테고리 단위로만 존재하고 '이번 달 전체적으로 예산 안에서 쓰고 있는가'를 한눈에 보여주는 집계 뷰가 없다는 점을 발견 — openSpendAnalysis()의 '총 지출' 카드(3524줄)는 예산 개념 없이 순수 합계만 보여준다. rateStatusText()/autoSyncRates()의 환율 동기화 실패 처리, delAsset()의 반복거래 연쇄 비활성화, importData()의 검증 등 다른 후보들은 이미 견고하게 구현돼 있음을 확인하고 제외했다. 코드 변경 없음 — pending_advance_plan에 예산 총액 요약 카드 계획을 적어둠.",
      "commit": null
    },
    {
      "cycle": 8,
      "phase": "review",
      "timestamp": "2026-09-10T17:30:00+00:00",
      "summary": "Reviewed commit 8121371 (yearly recDates() Feb-29 clamp fix). Traced through the rewritten loop by hand: mo/day are captured once from rs, d2=Math.min(day,lastDay(y,mo)) correctly clamps per-year (lastDay(y,m)=new Date(y,m,0).getDate() is the same helper already used by the monthly branch and addMonths()), the break condition new Date(y,mo-1,d2)>end terminates since the constructed date strictly increases with y, and ds>=r.startDate/push()'s own from/to/endDate bounds are unchanged from before so windowing behavior is preserved. Checked all 5 call sites of recDates() (index.html:1705,1725,1730,2511,4573) — all consume the returned array the same way as before (forEach/.length/direct array use), so the return type/shape didn't change. Re-ran the regression suite (45/45 pass) and re-validated both inline <script> blocks parse via new Function(). No bugs found — no code changes this phase. Also backfilled the cycle 8 develop history entry's commit hash (was null, actual is 8121371).",
      "commit": null
    },
    {
      "cycle": 8,
      "phase": "develop",
      "timestamp": "2026-09-10T00:00:00+00:00",
      "summary": "Fixed a real date-drift bug in recDates()'s yearly branch (index.html): it advanced occurrences with cd.setFullYear(cd.getFullYear()+1) on a native Date object, so a yearly recurrence starting on Feb 29 (e.g. an annual insurance premium or subscription) silently rolled over to Mar 1 on its first anniversary in a non-leap year and then stayed on Mar 1 forever, never returning to Feb 29 even in the next leap year — a genuine, permanent, one-time date corruption the user never asked for. This is the same class of month-end rollover bug the monthly branch right above it and addMonths() already guard against via Math.min(day,lastDay(y,m)) clamping (with an explicit '일자 롤오버 방지' comment at index.html:3071), but the yearly branch had never been given the same treatment. Rewrote the yearly branch to compute year/month/day explicitly and clamp the day via lastDay(y,mo) each iteration, mirroring the existing addMonths()/monthly-branch pattern instead of relying on Date rollover semantics — so a Feb-29 start now clamps to Feb 28 in non-leap years and correctly returns to Feb 29 in the next leap year. Added 3 new regression tests to test/run.js (permanent-drift-to-Mar-1 is fixed and clamps to 2/28 across 3 non-leap years, returns to 2/29 in the next leap year 2028, and a plain non-Feb-29 yearly date like 6/15 is unaffected and stays exact every year) — all 45 tests pass. Verified both inline <script> blocks still parse via new Function(). Bumped APP_VERSION 1.19.0→1.19.1 and sw.js CACHE v43→v44 (patch bump, bug fix).",
      "commit": "8121371"
    },
    {
      "cycle": 5,
      "phase": "develop",
      "timestamp": "2026-09-10T21:15:00+09:00",
      "summary": "Implemented the banked candidate from cycle 4's critique: assetSubline() was rounding fractional a.fxAmount/a.stockQty to integers via comma() (Math.round), while assetEval() already uses the unrounded values for net-worth totals — so a holding like 0.5주 or USD 123.45 would silently show as 0주/USD 123 in the asset list despite the true value being counted correctly in 총자산. Added commaQty(n): integers via comma(), non-integers via toLocaleString with maximumFractionDigits:4, matching how a.goldDon was already shown unrounded. Used it in assetSubline() for fxAmount/stockQty. Bumped APP_VERSION 1.17.2→1.17.3 and sw.js CACHE v37→v38.",
      "commit": "426cb47"
    },
    {
      "cycle": 5,
      "phase": "review",
      "timestamp": "2026-09-10T21:40:00+09:00",
      "summary": "Reviewed commit 426cb47 (commaQty display fix) — this exact fix was independently implemented in parallel by two concurrent app-evolve runs (the cloud routine and a local run), both picking up the same banked candidate from cycle 4's critique notes at nearly the same time. Reconciled via `git merge origin/main` (not reset, to avoid discarding either side's work): kept origin's already-pushed implementation as canonical, and merged in the genuinely additive part of the local work — regression test coverage. While wiring commaQty into test/run.js, found and fixed a real, separate bug in the test harness itself: extractConst() preserved the leading 'const ' keyword, but Node's vm.runInContext() does not attach top-level const/let bindings to the context object (only function declarations and plain assignments do), so anything routed through CONSTS was silently unreachable as sandbox.<name>. Stripped the 'const ' prefix so extracted consts become plain global assignments. Added 'comma'/'commaQty' to CONSTS and 3 new test cases. All 18 tests now pass. Lesson recorded for future concurrent-execution cycles: check git fetch/log origin/main immediately before push, prefer git merge over reset --hard (which the permission classifier blocks anyway).",
      "commit": "ee97922"
    },
    {
      "cycle": 5,
      "phase": "critique",
      "timestamp": "2026-09-10T22:00:00+09:00",
      "summary": "Stepped back from the bug-fixing pattern of the last several cycles to look for a genuine product gap rather than another small bug. Found one: DB.budgets is initialized as an empty object in migrate() but that single line is its ONLY reference anywhere in the codebase (verified via grep) — nothing ever reads or writes it, and the .budget-card CSS class is likewise defined but never used in any template. Meanwhile spendByCategory()/openSpendAnalysis() (the existing '지출 분석' sheet) already compute and display per-category spending for the month with a progress-bar UI, just relative to total spend rather than any budget limit. This is a half-built core feature of a budget-tracking app — the data model was scaffolded but the actual budget-setting UX was never connected. No code changed this phase (as intended for critique) — wrote a concrete plan to pending_advance_plan: extend the existing openSpendAnalysis() sheet (rather than building a new screen) to let the user tap a category row to set a monthly budget limit stored in DB.budgets[category], switch that row's progress bar to show usage-vs-budget (clamped, highlighted red when over 100%) while categories without a set budget keep today's behavior unchanged, and add regression tests for the percentage math including the divide-by-zero edge case.",
      "commit": null
    },
    {
      "cycle": 5,
      "phase": "advance",
      "timestamp": "2026-09-10T22:30:00+09:00",
      "summary": "Implemented cycle 5's critique plan: connected the previously-dead DB.budgets scaffolding to a real feature by extending the existing openSpendAnalysis() ('지출 분석') sheet instead of building a new screen. Tapping a category row now opens openBudgetPrompt(idx), which reads/writes DB.budgets[category] via saveBudget()/delBudget(). Extracted the percentage math into a pure budgetProgress(spent,budget) helper: when a budget is set, the row's progress bar switches from 'share of total spend' to 'usage vs budget' (clamped to 100% width so the bar never overflows), turns from --brand to --expense once spending exceeds the budget, and the amount/percentage text also gets the --expense highlight; categories with no budget keep today's total-share behavior unchanged. doRenameCat() now migrates DB.budgets[old]->DB.budgets[new] on expense-category rename, mirroring the existing catIcon/catVar migration pattern (delCat leaves orphaned budget entries alone, same as it already does for catIcon/catVar). Added 6 new regression tests (budgetProgress edge cases including the 0-budget divide-by-zero guard and the >100% clamp, plus the rename-migrates-budget case) — all 24 tests pass. Bumped APP_VERSION 1.17.3->1.17.4 and sw.js CACHE v38->v39.",
      "commit": "af5cd70"
    },
    {
      "cycle": 6,
      "phase": "develop",
      "timestamp": "2026-09-10T23:00:00+09:00",
      "summary": "delCat(k,i) only spliced the category out of DB.categories[k] but never cleaned up DB.catIcon[catKey(k,c)], DB.catVar[catKey(k,c)], or (for expense) DB.budgets[c] — the exact same class of orphaned-parallel-map bug that doRenameCat already had to fix for rename, but delete was never given the same treatment. Since catKey is purely name-based, deleting a category (e.g. '커피' with a custom icon, a '변동' flag, and a 30,000원 budget) and later re-adding one with the same name silently inherited the old icon/variable-flag/budget the user never set. Extracted the deletion body into doDeleteCat(k,i), mirroring doRenameCat's migration pattern, which now also deletes the catIcon/catVar entries and (for k==='expense') the budgets entry for the deleted category name; delCat() just calls it inside the existing confirmSheet callback. Added 3 new regression tests to test/run.js (icon/var/budget cleanup on delete, no inheritance on same-name re-add, and a guard that non-expense types don't touch DB.budgets) — all 27 tests pass. Verified both inline <script> blocks still parse via `new Function()`. Bumped APP_VERSION 1.17.4→1.17.5 and sw.js CACHE v39→v40.",
      "commit": "6aba2c4"
    },
    {
      "cycle": 6,
      "phase": "review",
      "timestamp": "2026-09-10T23:30:00+09:00",
      "summary": "Reviewed commit 6aba2c4 (doDeleteCat cleanup fix). Verified doDeleteCat mirrors doRenameCat's existing catIcon/catVar/budgets migration pattern correctly (same catKey-based lookups, same expense-only guard for DB.budgets), confirmed migrate() always initializes DB.catIcon/catVar/budgets so the new deletes are never against undefined objects, checked there is no second category-deletion code path that bypasses doDeleteCat (delCat is the only caller of DB.categories[k].splice), re-ran the regression suite (27/27 pass) and re-validated both inline <script> blocks parse via new Function(). No bugs found — no code changes this phase.",
      "commit": null
    },
    {
      "cycle": 6,
      "phase": "critique",
      "timestamp": "2026-09-10T23:45:00+09:00",
      "summary": "Stepped back to look at the whole app rather than the last commit. Found the app has no net-worth history/trend view at all — only ever shows a single 'now' snapshot — and the codebase itself shows this was started and abandoned: orphaned comments at index.html lines 159/167/1912/1914 ('① 현금흐름 곡선 카드', '현금흐름 곡선 그리기 (canvas)') plus a dead .fc-chart canvas CSS rule, with zero implementing function or <canvas> element anywhere (confirmed via grep). Also noted a structural gap that constrains the fix: DB.rates only stores a current price snapshot, no history, so any trend implementation must snapshot already-evaluated totals (via assetEval()/netWorth()) rather than trying to recompute past non-cash asset values from today's rates. Runner-up candidates noted but not chosen: accessibility is essentially absent (5 total aria-/role=/alt=/tabindex hits across 5172 lines), local auth password hashing is an unsalted djb2 hash (low real-world risk, local-only), and there's no CSV/spreadsheet export (JSON backup/restore only). Wrote the net-worth-history plan to pending_advance_plan for the next advance cycle. No code changed this phase.",
      "commit": null
    },
    {
      "cycle": 6,
      "phase": "advance",
      "timestamp": "2026-09-10T23:55:00+09:00",
      "summary": "Implemented cycle 6's critique plan: connected the net-worth history gap by adding DB.nwHistory=DB.nwHistory||[] to migrate() and two pure functions — updateNwHistory(hist,dateStr,ta,td) (append-or-update-today, immutable) and pruneNwHistory(hist) (keeps daily granularity for the most recent 90 days, compresses anything older to one snapshot per month, only kicks in above 120 entries so normal usage never touches it). Wired the snapshot into startApp() (covers first-ever open and every reopen, refreshing today's entry) and into refreshToday()'s day-changed branch (covers the app staying open across midnight) — both call save() right after, matching the existing changed-then-save() convention. Added nwChartPath(pts,W,H), a pure function producing an SVG path string (min/max-normalized, span-zero-safe) with no chart library, and nwHistoryCard() which renders it as an inline <svg> sparkline card (shown only once ≥2 snapshots exist) inserted into renderAssets() right after the net-worth carousel. Replaced the entire dead cash-flow-curve CSS block (.flow-card/.fc-head/.fc-lb/.fc-end/.fc-sub/.fc-chart/.fc-foot/.fc-stat/.fs-k/.fs-v — verified via grep that none of these classes were referenced anywhere outside their own definitions) with real .nwh-card/.nwh-range/.nwh-chart rules, and replaced the orphaned '① 현금흐름 곡선 카드' / '현금흐름 곡선 그리기 (canvas)' comments with the real nwHistoryCard() implementation, putting the previously-unused shortDate2() helper to actual use. Added 8 new regression tests (updateNwHistory append/update-in-place/immutability, pruneNwHistory below-threshold and month-compression behavior, nwChartPath's <2-points/flat-line/rising-line cases) — all 35 tests pass. Verified both inline <script> blocks still parse via new Function(). Bumped APP_VERSION 1.17.5→1.18.0 and sw.js CACHE v40→v41 (minor bump, not patch, since this adds a new schema field and a new UI section rather than fixing a bug).",
      "commit": null
    },
    {
      "cycle": 7,
      "phase": "develop",
      "timestamp": "2026-09-10T13:15:00+00:00",
      "summary": "addCat(k) was the odd one out among the app's 'add/rename named item' flows: addOwner() and doRenameCat() both check for a duplicate name and toast('이미 있는 카테고리예요'/'이미 있는 귀속이에요') before returning, but addCat() just silently skipped the DB.categories[k].push() on a duplicate and still cleared the draft, saved, and reopened the sheet — giving the user zero feedback that nothing happened when they typed an existing category name. Worse, if the user had picked a custom icon in catAddDraft.icon first, the DB.catIcon[catKey(k,v)]=catAddDraft.icon line still ran unconditionally, silently overwriting the icon of the pre-existing category with the same name. Fixed by adding the same early-return-with-toast guard used by addOwner/doRenameCat, which also fixes the icon-overwrite side effect since the function now returns before touching catIcon. Added addCat to the test harness's FUNCTIONS list and gave the sandbox toast() a lastToast recorder (previously a no-op) plus a catAddDraft field so it could be exercised; added 2 new regression tests (duplicate name toasts and leaves array/icon untouched; new name adds normally with no toast) — all 37 tests pass. Verified both inline <script> blocks still parse via new Function(). Bumped APP_VERSION 1.18.0→1.18.1 and sw.js CACHE v41→v42.",
      "commit": "efe88da"
    },
    {
      "cycle": 7,
      "phase": "review",
      "timestamp": "2026-09-10T13:20:00+00:00",
      "summary": "Reviewed commit efe88da (addCat duplicate-name toast fix). Confirmed the early-return guard matches addOwner/doRenameCat's existing pattern exactly, verified the icon-overwrite side effect is genuinely fixed (function now returns before touching DB.catIcon), checked there's no other code path that calls DB.categories[k].push() bypassing addCat, and confirmed migrate() always initializes DB.categories[k] as an array so .includes() is never called on undefined. Re-ran the regression suite (37/37 pass) and re-validated both inline <script> blocks parse via new Function(). No bugs found — no code changes this phase.",
      "commit": null
    },
    {
      "cycle": 7,
      "phase": "critique",
      "timestamp": "2026-09-10T22:10:00+09:00",
      "summary": "Stepped back to look at the whole app rather than the last commit. Data export is JSON-backup-only: exportData()/doExport() (index.html ~3956-4015) produce a full-DB JSON blob meant purely for restoring into this same app — confirmed via grep that 'csv'/'CSV' appear zero times anywhere in index.html. There is no way to get transaction history into a spreadsheet, which is a normal thing to want from a finance tracker and was already flagged as a runner-up in cycle 6's critique (still untouched). Checked other suspects first and ruled them out as either already solid or not scoped for one cycle: delAsset() already snapshots asset names into txns/recurrences and has a relink path for re-added same-name assets (no orphan bug there, unlike the delCat bug fixed in cycle 6); importData() already validates obj.assets/obj.txns before replacing DB and calls migrate() after; the history search/filter UI (histQ, ST.hist) is already implemented, not a stub. Accessibility (still ~5 aria/role/alt/tabindex hits total) and the unsalted djb2 local-auth hash remain real but diffuse/low-priority runner-ups, same as last cycle. Picked CSV export because the JSON export's share/download/copy-fallback plumbing already exists and can be reused as-is for a new payload — a 'connect infra to a new capability' shape that fits one advance cycle, plus it has genuine pure-function surface (CSV field escaping for commas/quotes/newlines in free-text memos) worth regression-testing. No code changed this phase — wrote the CSV-export plan to pending_advance_plan for the next advance cycle.",
      "commit": null
    },
    {
      "cycle": 7,
      "phase": "advance",
      "timestamp": "2026-09-10T00:00:00+00:00",
      "summary": "Implemented cycle 7's critique plan: added transaction-history CSV export alongside the existing JSON backup. Added a pure txnsToCSV(txns,assets) function (RFC4180 field escaping — quotes any field containing a comma/quote/newline and doubles embedded quotes; UTF-8 BOM prepended so Excel renders 한글 correctly; resolves fromAssetId/toAssetId to names via the assets array, falling back to the fromAssetName/toAssetName snapshot fields for deleted assets, same pattern assetNm() already uses; sorted by date ascending). Wired it into exportData() as a third button ('거래 내역 CSV로 내보내기') that picks share vs. download automatically based on device capability, reusing doExport()'s existing branching — generalized doExport(mode) to doExport(mode,kind) so both JSON and CSV share the same share/download/copy-fallback code paths, without touching or renaming the existing JSON backup path. The CSV path does not update DB.settings.lastExport/backupSnooze since that nag tracking is specific to the JSON-restore backup, not this. Also parameterized openCopyBackup()'s title/note text (via an optional override arg, JSON behavior unchanged when omitted) so the last-resort copy-to-clipboard fallback shows CSV-appropriate guidance ('.csv 파일로 저장') instead of the JSON-restore wording when it's the CSV export that failed to share/download. Caught and fixed a self-introduced syntax bug during implementation (a stray closing backtick that terminated the exportData() template literal early) by re-verifying both inline <script> blocks parse via vm.Script before committing — this is exactly the class of bug that check exists to catch. Added 5 new regression tests for txnsToCSV (empty-list header-only output, comma-in-memo quoting, double-quote-in-memo escaping, live-asset vs. deleted-asset-snapshot-name resolution, date-ascending sort) — all 42 tests pass. Bumped APP_VERSION 1.18.1→1.19.0 (minor, new feature) and sw.js CACHE v42→v43.",
      "commit": null
    }
  ]
}
